
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: helloworld
  namespace: default
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - http: &http_rules
      paths:
      - backend:
          serviceName: hello-kubernetes
          servicePort: 80
